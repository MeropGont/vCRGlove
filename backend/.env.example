# Copy this file to .env and fill in the values.
# Never commit .env to version control.

# A strong random string — share only with the iOS app config and the dashboard.
VCR_API_KEY=replace_with_a_long_random_string
